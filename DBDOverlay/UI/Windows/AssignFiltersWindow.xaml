<Window x:Class="DBDOverlay.UI.Windows.AssignFiltersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DBDOverlay.UI.Windows"
        mc:Ignorable="d"
        Title="AssignFilters" Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Border Background="{DynamicResource DarkestGrayBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.88*"/>
                <RowDefinition Height="0.12*"/>
            </Grid.RowDefinitions>
            <ScrollViewer x:Name="FiltersScrollViewer"
                          Grid.Row="0"
                          HorizontalAlignment="Center"
                          Margin="0,10,0,0"
                          Padding="5"
                          Style="{StaticResource ScrollViewerStyle}">
                <Grid x:Name="MapFiltersGrid">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                    </Grid.RowDefinitions>
                </Grid>
            </ScrollViewer>

            <Button Grid.Row="1"
                    x:Name="ResetButton"
                    Style="{StaticResource ButtonStyle}"
                    Content="Reset"
                    FontSize="16"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Width="80"
                    Height="30"
                    Margin="0,0,0,5"
                    Click="Reset_Click"/>

            <Button Grid.Row="1"
                    x:Name="CloseButton"
                    Style="{StaticResource ButtonStyle}"
                    Content="Close"
                    FontSize="16"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right"
                    Width="60"
                    Height="30"
                    Margin="0,0,10,5"
                    Click="Close_Click">
            </Button>
        </Grid>
    </Border>
</Window>
